---
phase: 07-thread-management-resolution
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - packages/ui/components/SummaryModal.tsx
  - packages/ui/components/ThreadPickerModal.tsx
  - packages/ui/components/AnnotationPanel.tsx
  - packages/ui/hooks/useSummaryAnnotation.ts
  - packages/ui/hooks/useThreadNav.ts
  - packages/ui/utils/summaryExport.ts
autonomous: true
requirements: [THREAD-01, THREAD-02, THREAD-07]
must_haves:
  truths:
    - "User can click 'Summarize' on a thread parent annotation and create a summary"
    - "Thread picker modal shows all threads when triggered from toolbar"
    - "Summary annotations display with yellow tint, left border, and 'Summary' badge"
    - "Resolved threads show 'Resolved' badge with 70% opacity on thread parent"
    - "User can toggle 'Show resolved' checkbox to filter resolved threads"
    - "User can navigate between threads with Previous/Next thread buttons"
    - "User can export all summaries as markdown document"
  artifacts:
    - path: "packages/ui/components/SummaryModal.tsx"
      provides: "Summary creation modal with textarea and thread context"
      exports: ["SummaryModal"]
    - path: "packages/ui/components/ThreadPickerModal.tsx"
      provides: "Thread picker for toolbar trigger path"
      exports: ["ThreadPickerModal"]
    - path: "packages/ui/components/AnnotationPanel.tsx"
      provides: "Summarize button, resolved badge, thread filter, thread nav"
    - path: "packages/ui/hooks/useSummaryAnnotation.ts"
      provides: "Summary creation and thread identification logic"
      exports: ["useSummaryAnnotation"]
    - path: "packages/ui/hooks/useThreadNav.ts"
      provides: "Thread jump navigation state"
      exports: ["useThreadNav"]
    - path: "packages/ui/utils/summaryExport.ts"
      provides: "Markdown export for summaries"
      exports: ["exportSummariesAsMarkdown"]
  key_links:
    - from: "packages/ui/components/AnnotationPanel.tsx"
      to: "packages/ui/components/SummaryModal.tsx"
      via: "Summarize button onClick opens modal"
      pattern: "SummaryModal"
    - from: "packages/ui/hooks/useSummaryAnnotation.ts"
      to: "packages/ui/types.ts"
      via: "Creates Annotation with isSummary=true"
      pattern: "isSummary.*true"
---

<objective>
Build the summary annotation creation UI, thread navigation, resolved thread display, and markdown export.

Purpose: This plan delivers all the annotation panel enhancements -- the core user-facing thread management experience. Users will be able to create summaries, see resolved status, navigate threads, and export decisions.
Output: SummaryModal, ThreadPickerModal, thread nav buttons, resolved badges, filter toggle, export function.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-thread-management-resolution/07-CONTEXT.md
@.planning/phases/07-thread-management-resolution/07-RESEARCH.md
@.planning/phases/07-thread-management-resolution/07-UI-SPEC.md
@.planning/phases/07-thread-management-resolution/07-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output -->

From packages/ui/types.ts (extended):
```typescript
export interface Annotation {
  // ...existing fields
  children?: Annotation[];
  githubCommentUrl?: string;
  isSummary?: boolean;           // NEW from Plan 01
  summarizesThreadId?: string;   // NEW from Plan 01
  isResolved?: boolean;          // NEW from Plan 01
}
```

From packages/ui/components/AnnotationPanel.tsx (current exports):
```typescript
export const AnnotationPanel: React.FC<PanelProps> = ({...})
// Renders annotations with recursive thread nesting at lines 593-612
// children rendered with margin-left indentation
```

From packages/ui/utils/parser.ts:
```typescript
export function exportAnnotations(blocks: Block[], annotations: Annotation[], globalAttachments?: ImageAttachment[]): string;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create summary hooks, modals, and export utility</name>
  <files>packages/ui/hooks/useSummaryAnnotation.ts, packages/ui/hooks/useThreadNav.ts, packages/ui/components/SummaryModal.tsx, packages/ui/components/ThreadPickerModal.tsx, packages/ui/utils/summaryExport.ts</files>
  <read_first>
    - packages/ui/types.ts (Annotation with isSummary, summarizesThreadId, isResolved from Plan 01)
    - packages/ui/components/AnnotationPanel.tsx (thread rendering pattern, annotation card structure, existing button patterns)
    - packages/ui/components/ExportModal.tsx (modal overlay pattern to reuse)
    - packages/ui/theme.css (CSS variable names for themed colors)
    - packages/github/client/threadTree.ts (buildThreadTree for identifying threads)
    - packages/ui/hooks/useAnnotationDraft.ts (existing hook pattern reference)
  </read_first>
  <action>
    1. Create `packages/ui/hooks/useSummaryAnnotation.ts`:
       - `useSummaryAnnotation(annotations, onAddAnnotation)` hook
       - `getThreads(annotations)`: filters annotations where `children && children.length > 0` -- these are thread parents
       - `getThreadLabel(thread)`: returns first 50 chars of thread.originalText + "..." if longer (D-05)
       - `createSummary(threadParentId, summaryText, threadParent)`: creates a new Annotation object with:
         - `id`: use crypto.randomUUID() (client-side, stable ID generated on sync)
         - `blockId`: same as threadParent.blockId
         - `startOffset`: 0, `endOffset`: 0
         - `type`: AnnotationType.COMMENT
         - `text`: summaryText
         - `originalText`: summaryText
         - `isSummary`: true
         - `summarizesThreadId`: threadParentId
         - `createdA`: Date.now()
       - Calls onAddAnnotation with the new summary annotation
       - State: `summaryModalOpen`, `selectedThreadId`, `threadPickerOpen`
       - Methods: `openSummaryModal(threadId)`, `openThreadPicker()`, `closeSummaryModal()`, `closeThreadPicker()`, `submitSummary(text)`

    2. Create `packages/ui/hooks/useThreadNav.ts`:
       - `useThreadNav(annotations, panelRef)` hook
       - `threadRoots`: computed list of thread parent annotation IDs (annotations with children)
       - `currentIndex`: tracks which thread is currently focused (-1 = none)
       - `goToNext()`: increments index, scrolls to next thread root element, highlights with `bg-accent/15` for 1.5s (D-08)
       - `goToPrev()`: decrements index, scrolls to prev thread root
       - `hasNext`, `hasPrev`: boolean flags for button disabled state
       - Scroll via `document.querySelector([data-annotation-id="{id}"])?.scrollIntoView({ behavior: 'smooth', block: 'center' })`
       - Highlight via adding/removing a CSS class after 1.5s timeout

    3. Create `packages/ui/components/SummaryModal.tsx`:
       - Props: `isOpen`, `onClose`, `threadLabel` (string, preview text), `onSubmit(text: string)`
       - Overlay: `fixed inset-0 bg-background/80 backdrop-blur-sm z-50` (per UI-SPEC)
       - Modal container: `bg-card border border-border rounded-lg shadow-lg max-w-lg w-full mx-auto mt-24 p-4`
       - Title: "Summarize Thread" (`text-sm font-semibold`)
       - Thread context: `text-xs text-muted-foreground mb-2` showing "Summary of: {threadLabel}" (D-22)
       - Textarea: `w-full min-h-[120px] bg-input border border-border rounded-md p-2 text-sm text-foreground placeholder:text-muted-foreground`
       - Placeholder: "Capture the decision from this discussion..." (UI-SPEC)
       - Buttons: flex justify-end gap-2 mt-4
         - "Create Summary" button: `bg-accent text-accent-foreground px-4 py-2 rounded-md text-xs font-semibold` -- disabled when textarea empty
         - "Cancel" button: `bg-muted text-muted-foreground px-4 py-2 rounded-md text-xs font-semibold`
       - Close on Escape key and overlay click

    4. Create `packages/ui/components/ThreadPickerModal.tsx`:
       - Props: `isOpen`, `onClose`, `threads` (array of { id, label, replyCount, lastActivity }), `onSelect(threadId: string)`
       - Same overlay pattern as SummaryModal
       - Modal: `bg-card border border-border rounded-lg shadow-lg max-w-md w-full mx-auto mt-24`
       - Title: "Select Thread to Summarize" in bordered header (D-07)
       - Thread list: `max-h-[400px] overflow-y-auto` scrollable container
       - Each thread item: `flex items-center justify-between p-4 border-b border-border/50 hover:bg-muted/50 cursor-pointer`
         - Label: `text-sm text-foreground truncate` -- first 50 chars + "..."
         - Meta: `text-xs text-muted-foreground` -- "{N} replies"
         - "Summarize" link: `text-xs font-semibold text-accent hover:underline`
       - Empty state: "No active threads to summarize" centered (UI-SPEC)
       - On select: close picker, open SummaryModal with thread context

    5. Create `packages/ui/utils/summaryExport.ts`:
       - `exportSummariesAsMarkdown(annotations, prNumber?, prTitle?)`: returns markdown string (D-43)
       - Format per CONTEXT.md specifics:
         ```
         # PR Review Summaries\n\n
         **PR:** #{prNumber} - {prTitle}\n
         **Generated:** {ISO date}\n\n---\n\n
         ## Thread {N}\n\n
         **Discussion:** {first 50 chars of thread root}...\n\n
         **Summary:** {summary text}\n\n
         **Status:** {Resolved|Active}\n\n---\n\n
         ```
       - Collect all annotations where `isSummary === true`
       - For each, find thread parent via `summarizesThreadId` in the flat annotations list
       - `downloadSummariesMarkdown(markdown, prNumber?)`: creates blob URL and triggers download with filename `pr-{number}-summaries-{YYYY-MM-DD}.md` (UI-SPEC)
  </action>
  <verify>
    <automated>cd /Users/ssiddhm5/Documents/NDProjects/plannotator && grep -l "useSummaryAnnotation\|SummaryModal\|ThreadPickerModal\|useThreadNav\|exportSummariesAsMarkdown" packages/ui/hooks/useSummaryAnnotation.ts packages/ui/hooks/useThreadNav.ts packages/ui/components/SummaryModal.tsx packages/ui/components/ThreadPickerModal.tsx packages/ui/utils/summaryExport.ts</automated>
  </verify>
  <acceptance_criteria>
    - packages/ui/hooks/useSummaryAnnotation.ts contains `export function useSummaryAnnotation`
    - packages/ui/hooks/useSummaryAnnotation.ts contains `isSummary: true`
    - packages/ui/hooks/useSummaryAnnotation.ts contains `summarizesThreadId`
    - packages/ui/hooks/useThreadNav.ts contains `export function useThreadNav`
    - packages/ui/hooks/useThreadNav.ts contains `goToNext` and `goToPrev`
    - packages/ui/components/SummaryModal.tsx contains `Summarize Thread`
    - packages/ui/components/SummaryModal.tsx contains `Capture the decision from this discussion`
    - packages/ui/components/SummaryModal.tsx contains `Create Summary`
    - packages/ui/components/ThreadPickerModal.tsx contains `Select Thread to Summarize`
    - packages/ui/components/ThreadPickerModal.tsx contains `No active threads to summarize`
    - packages/ui/utils/summaryExport.ts contains `export function exportSummariesAsMarkdown`
    - packages/ui/utils/summaryExport.ts contains `PR Review Summaries`
    - packages/ui/utils/summaryExport.ts contains `downloadSummariesMarkdown`
  </acceptance_criteria>
  <done>Summary creation hooks, modals, thread nav hook, and export utility all exist with correct interfaces. Ready for AnnotationPanel integration.</done>
</task>

<task type="auto">
  <name>Task 2: Extend AnnotationPanel with summarize button, resolved badges, thread filter, thread nav, and export button</name>
  <files>packages/ui/components/AnnotationPanel.tsx</files>
  <read_first>
    - packages/ui/components/AnnotationPanel.tsx (full file -- understand current structure, thread rendering at lines 593-612, header area, button patterns)
    - packages/ui/hooks/useSummaryAnnotation.ts (from Task 1)
    - packages/ui/hooks/useThreadNav.ts (from Task 1)
    - packages/ui/components/SummaryModal.tsx (from Task 1)
    - packages/ui/components/ThreadPickerModal.tsx (from Task 1)
    - packages/ui/utils/summaryExport.ts (from Task 1)
    - packages/ui/types.ts (Annotation with isSummary, isResolved, summarizesThreadId)
    - packages/ui/theme.css (CSS variables)
    - .planning/phases/07-thread-management-resolution/07-UI-SPEC.md (exact styling specs)
  </read_first>
  <action>
    Extend `packages/ui/components/AnnotationPanel.tsx` with six additions. All styling uses CSS variables from theme.css via Tailwind utility classes per UI-SPEC.

    1. **Summarize button on thread parents** (D-01, D-02):
       - In the annotation card header/action row, after existing action buttons (edit/delete), add a "Summarize" button
       - Only render when `annotation.children && annotation.children.length > 0`
       - Button: clipboard-check SVG icon (16x16, stroke="currentColor" strokeWidth={2}) + "Summarize" text
       - Styling: `text-muted-foreground hover:text-accent hover:bg-accent/10 rounded-md px-2 py-1 text-xs font-semibold`
       - onClick: call `summaryHook.openSummaryModal(annotation.id)` (auto-detects thread per D-02)

    2. **Summary annotation card styling** (D-21, D-22, D-23, D-24):
       - When rendering an annotation where `annotation.isSummary === true`:
         - Container: add `bg-warning/10 border-l-4 border-warning pl-3`
         - Badge: before author/timestamp, show `<span className="bg-warning text-warning-foreground text-[10px] font-mono font-semibold px-1.5 py-0.5 rounded inline-flex items-center gap-1">` with clipboard-check icon (12x12) + "Summary"
         - Thread context: `<div className="text-xs text-muted-foreground">Summary of: {getThreadLabel(parentAnnotation)}</div>` above body text
       - Summary annotations appear as last child in thread (handled by creation order in useSummaryAnnotation)

    3. **Resolved thread badge** (D-17, D-20):
       - On thread parent annotations where `annotation.isResolved === true`:
         - Badge: `<span className="bg-success text-success-foreground text-[10px] font-mono font-semibold px-1.5 py-0.5 rounded">Resolved</span>` in card header
         - Entire thread container: add `opacity-70` class (D-17)
       - Badge on thread parent only, not on children (D-20)

    4. **Resolved thread filter** (D-19):
       - Add `const [showResolved, setShowResolved] = useState(true)` (per-session, checked by default)
       - In panel header, right side after annotation count: checkbox + "Show resolved" label
       - Checkbox: `<input type="checkbox" checked={showResolved} onChange={e => setShowResolved(e.target.checked)} className="w-3.5 h-3.5 rounded-sm border border-border accent-accent" />`
       - Label: `<span className="text-[10px] font-mono text-muted-foreground ml-1">Show resolved</span>`
       - When unchecked, filter out annotations where `annotation.isResolved === true` from the rendered list

    5. **Thread jump navigation** (D-08):
       - Import and use `useThreadNav` hook
       - In panel header row, add navigation buttons:
       - Prev button: chevron-up SVG (14x14), `p-1 rounded-md text-muted-foreground hover:text-foreground hover:bg-muted`
       - Next button: chevron-down SVG (14x14), same styling
       - Disabled state: `opacity-50 cursor-not-allowed` when `!hasPrev` / `!hasNext`
       - Title attributes: "Previous thread" / "Next thread"
       - onClick: `threadNav.goToPrev()` / `threadNav.goToNext()`

    6. **Export summaries button** (D-43):
       - In panel header or below nav buttons: export button
       - Icon: arrow-down-tray SVG (16x16)
       - Label: "Export Summaries"
       - Styling: `text-muted-foreground hover:text-accent text-xs`
       - Disabled: `opacity-50 cursor-not-allowed` when no annotations have `isSummary === true`
       - onClick: call `downloadSummariesMarkdown(exportSummariesAsMarkdown(annotations, prNumber, prTitle), prNumber)`

    7. **Wire modals**: Render `<SummaryModal>` and `<ThreadPickerModal>` at the bottom of the component, controlled by `useSummaryAnnotation` hook state.

    IMPORTANT: Add `data-annotation-id={annotation.id}` attribute to each annotation card wrapper div, so useThreadNav can find elements to scroll to.

    Props: The AnnotationPanel will need new optional props: `onAddAnnotation?: (annotation: Annotation) => void` and `prMetadata?: { prNumber: number; prTitle?: string }` (for export). Check existing prop interface and extend minimally.

    **Decision constraints for this task:**
    - Per D-03: Summary annotations must be editable like regular comments (ensure summary cards render with same edit/delete actions as normal annotations)
    - Per D-06: Multiple threads on the same line remain independent -- each thread parent gets its own Summarize button
    - Per D-10: No confirmation dialogs before resolution -- sync proceeds without "Are you sure?" prompts
    - Per D-12: No "Unresolve" button in Plannotator -- unresolve only happens on GitHub directly
    - Per D-18: Resolved threads stay expanded by default -- do NOT auto-collapse resolved threads
  </action>
  <verify>
    <automated>cd /Users/ssiddhm5/Documents/NDProjects/plannotator && grep -c "Summarize\|isResolved\|isSummary\|Show resolved\|Previous thread\|Next thread\|Export Summaries\|SummaryModal\|ThreadPickerModal\|useThreadNav\|useSummaryAnnotation" packages/ui/components/AnnotationPanel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - packages/ui/components/AnnotationPanel.tsx contains `Summarize` button text
    - packages/ui/components/AnnotationPanel.tsx contains `isSummary` check
    - packages/ui/components/AnnotationPanel.tsx contains `isResolved` check
    - packages/ui/components/AnnotationPanel.tsx contains `Show resolved` label
    - packages/ui/components/AnnotationPanel.tsx contains `Previous thread` and `Next thread` tooltip text
    - packages/ui/components/AnnotationPanel.tsx contains `Export Summaries` button text
    - packages/ui/components/AnnotationPanel.tsx contains `bg-warning/10` (summary card background)
    - packages/ui/components/AnnotationPanel.tsx contains `bg-success` (resolved badge)
    - packages/ui/components/AnnotationPanel.tsx contains `opacity-70` (resolved thread dimming)
    - packages/ui/components/AnnotationPanel.tsx contains `data-annotation-id` attribute
    - packages/ui/components/AnnotationPanel.tsx imports SummaryModal and ThreadPickerModal
    - packages/ui/components/AnnotationPanel.tsx imports useSummaryAnnotation and useThreadNav
    - grep count returns 13+ (all features present)
  </acceptance_criteria>
  <done>AnnotationPanel has summarize button on thread parents, summary card styling, resolved badges, thread filter, thread navigation, export button, and modals wired. All per UI-SPEC and CONTEXT.md decisions.</done>
</task>

</tasks>

<verification>
- All 5 new files exist and export their main functions/components
- AnnotationPanel.tsx integrates all new components and hooks
- UI-SPEC copywriting matches exactly: "Summarize", "Summary", "Resolved", "Show resolved", "Previous thread", "Next thread", "Export Summaries"
- No new npm dependencies added
</verification>

<success_criteria>
- User can see "Summarize" button on thread parent annotations
- Summary creation modal opens with thread context and textarea
- Thread picker modal lists all active threads
- Summary annotations render with yellow tint and "Summary" badge
- Resolved threads show "Resolved" badge with dimmed opacity
- Thread filter toggles resolved thread visibility
- Thread nav buttons scroll between thread roots
- Export summaries button generates markdown download
</success_criteria>

<output>
After completion, create `.planning/phases/07-thread-management-resolution/07-02-SUMMARY.md`
</output>
