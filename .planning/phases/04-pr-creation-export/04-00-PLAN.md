---
phase: 04-pr-creation-export
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/github/server/export.test.ts
  - packages/github/shared/planHash.test.ts
autonomous: true
requirements: [PR-02, PR-03, PR-04, PR-05]
must_haves:
  truths:
    - "Test skeleton files exist before implementation tasks run"
    - "Test stubs define expected behavior without implementation"
  artifacts:
    - path: "packages/github/server/export.test.ts"
      provides: "Empty test stubs for mapAnnotationsToComments, submitBatchReview, exportPlanWithAnnotations"
      min_lines: 30
    - path: "packages/github/shared/planHash.test.ts"
      provides: "Empty test stubs for generatePlanHash"
      min_lines: 10
  key_links: []
---

<objective>
Create test skeleton files required by Wave 1 plans. These stubs define the expected behavior contracts that Plan 01 will implement against.

Purpose: Satisfy Nyquist Wave 0 requirement -- test files must exist before tasks that verify against them.
Output: export.test.ts and planHash.test.ts with empty test stubs
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-pr-creation-export/04-CONTEXT.md

<interfaces>
<!-- Behavior contracts from 04-01 Task 1 that define what these tests must cover -->

From packages/github/server/pr.ts:
```typescript
export async function githubRequest(endpoint: string, token: string, body?: any): Promise<any>;
```

From packages/github/shared/stableId.ts (SHA-256 pattern):
```typescript
export async function generateStableId(blockId: string, originalText: string): Promise<string>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test skeleton files with empty stubs for export and planHash modules</name>
  <files>packages/github/server/export.test.ts, packages/github/shared/planHash.test.ts</files>
  <action>
    **packages/github/shared/planHash.test.ts:**
    Create test file with `bun:test` imports (describe, test, expect). Add stubs:
    - `describe("generatePlanHash", () => { ... })` with:
      - `test("returns a 64-char hex string", () => { expect(true).toBe(true); /* TODO: implement */ })`
      - `test("is deterministic (same input = same hash)", () => { expect(true).toBe(true); /* TODO: implement */ })`

    **packages/github/server/export.test.ts:**
    Create test file with `bun:test` imports (describe, test, expect). Add stubs grouped by function:

    ```typescript
    describe("mapAnnotationsToComments", () => {
      test("COMMENT annotation returns path, line, side RIGHT, body", () => { expect(true).toBe(true); });
      test("DELETION annotation returns body with suggestion block", () => { expect(true).toBe(true); });
      test("GLOBAL_COMMENT annotation returns null (filtered out)", () => { expect(true).toBe(true); });
      test("annotation with missing block defaults to line 1", () => { expect(true).toBe(true); });
      test("annotation with images exports text only (per D-07)", () => { expect(true).toBe(true); });
    });

    describe("submitBatchReview", () => {
      test("calls githubRequest with POST reviews endpoint", () => { expect(true).toBe(true); });
      test("passes event COMMENT and comments array", () => { expect(true).toBe(true); });
      test("submits review with just body when comments empty", () => { expect(true).toBe(true); });
    });

    describe("exportPlanWithAnnotations", () => {
      test("calls exportToPR then submitBatchReview then stores metadata", () => { expect(true).toBe(true); });
      test("rolls back branch on review submission failure", () => { expect(true).toBe(true); });
      test("skips review when no line-level annotations exist", () => { expect(true).toBe(true); });
      test("includes GLOBAL_COMMENT text in review body", () => { expect(true).toBe(true); });
    });
    ```

    All stubs pass trivially (`expect(true).toBe(true)`) -- Plan 01 Task 1 will replace these with real assertions during TDD RED phase.
  </action>
  <verify>
    <automated>cd /Users/ssiddhm5/Documents/NDProjects/plannotator && bun test packages/github/server/export.test.ts packages/github/shared/planHash.test.ts --reporter=default 2>&1 | tail -5</automated>
  </verify>
  <done>Both test skeleton files exist with all expected test stubs. `bun test` runs them and all stubs pass (trivially). Plan 04-01 can now reference these files in its verify commands.</done>
</task>

</tasks>

<verification>
- `bun test packages/github/server/export.test.ts` exits 0
- `bun test packages/github/shared/planHash.test.ts` exits 0
- `wc -l packages/github/server/export.test.ts` shows >= 30 lines
</verification>

<success_criteria>
- Test skeleton files exist at expected paths
- All stubs pass trivially (no red tests blocking Wave 1)
- Stubs cover all behavior items from Plan 04-01 Task 1
</success_criteria>

<output>
After completion, create `.planning/phases/04-pr-creation-export/04-00-SUMMARY.md`
</output>
