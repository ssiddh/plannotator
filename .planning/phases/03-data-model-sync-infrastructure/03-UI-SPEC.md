---
phase: 3
slug: data-model-sync-infrastructure
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-02
---

# Phase 3 — UI Design Contract

> Visual and interaction contract for Phase 3: Data Model & Sync Infrastructure.
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Phase UI Classification: Infrastructure (Minimal UI Surface)

Phase 3 is explicitly a data infrastructure phase. RESEARCH.md states: "entirely about data structures and pure functions -- no UI, no API endpoints, no network calls." The phase produces TypeScript modules (stable ID generation, KV mapping, sync state, conflict detection) that downstream phases (4-7) consume.

**This UI-SPEC defines:**
1. Copywriting contracts for UI copy that downstream phases will render
2. Data shape contracts that constrain future UI (conflict info, drift warning)
3. Confirmation that existing design system tokens are sufficient (no new tokens needed)

**This UI-SPEC does NOT define:** New components, layouts, spacing overrides, or typography changes. The existing Plannotator theme system and component library are unchanged by this phase.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (existing custom theme system) |
| Preset | not applicable |
| Component library | custom React components in `packages/ui/components/` |
| Icon library | inline SVG (existing pattern) |
| Font | Inter (sans), JetBrains Mono (mono) -- via CSS vars `--font-sans`, `--font-mono` |

Source: `packages/ui/theme.css` and `packages/ui/themes/plannotator.css` (18 theme variants exist).

---

## Spacing Scale

No new spacing tokens required. Phase 3 produces no rendered UI. Downstream phases consuming Phase 3 data structures will use the existing 8-point scale already established in the project:

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding |
| sm | 8px | Compact element spacing |
| md | 16px | Default element spacing |
| lg | 24px | Section padding |
| xl | 32px | Layout gaps |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level spacing |

Exceptions: none

---

## Typography

No new typography tokens required. Downstream phases rendering drift warnings and conflict dialogs will use existing type scale:

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px | 400 (regular) | 1.5 |
| Label | 12px | 500 (medium) | 1.4 |
| Heading | 16px | 600 (semibold) | 1.2 |

Source: Inferred from existing component patterns in `packages/ui/`. No new roles introduced by this phase.

---

## Color

No new color tokens required. Phase 3 data structures reference semantic states that map to existing theme tokens:

| Semantic State | Existing Token | Usage in Downstream UI |
|----------------|---------------|----------------------|
| Drift warning | `--warning` / `--warning-foreground` | Drift warning banner (D-09) |
| Conflict alert | `--destructive` / `--destructive-foreground` | Conflict detection indicator (D-12) |
| Sync success | `--success` / `--success-foreground` | Sync completion feedback |
| Neutral info | `--muted` / `--muted-foreground` | Sync metadata display |

All tokens already defined in every theme variant. No 60/30/10 split change.

Accent reserved for: No change. Existing accent usage (annotation highlights, active tab indicators) is unaffected.

---

## Copywriting Contract

These copy strings are defined now so downstream phases (5, 6, 7) can render them consistently. Phase 3 exports these as constants or type constraints.

| Element | Copy | Context |
|---------|------|---------|
| Drift warning heading | "Plan changed since PR creation" | Banner shown when plan hash mismatch detected (D-09) |
| Drift warning body | "Line numbers may be incorrect. Annotations might not land on the expected lines." | Follows heading in warning banner |
| Drift warning CTA (proceed) | "Sync anyway" | Allows user to continue despite drift. Confirmation: warning banner shows both options ("Sync anyway" and "Don't sync") visible simultaneously -- no modal, user must click an explicit choice to proceed |
| Drift warning CTA (cancel) | "Don't sync" | Aborts sync operation |
| Conflict heading | "Annotation changed in both places" | Modal shown when both sides modified since last sync (D-12) |
| Conflict body | "This annotation was modified locally and on GitHub since the last sync." | Explains the conflict situation |
| Conflict CTA (keep local) | "Keep local" | Preserves Plannotator version, skips GitHub import |
| Conflict CTA (keep remote) | "Keep GitHub" | Overwrites local with GitHub comment content. Confirmation: modal dialog shows side-by-side comparison of local vs GitHub text before user commits to choice -- modal cannot be dismissed without selecting an option |
| Conflict CTA (cancel) | "Don't sync" | Aborts entire sync operation |
| Empty state (no sync history) | "No sync history" | Shown when paste has never been synced |
| Empty state body | "Sync annotations with a linked GitHub PR to see history here." | Guides user toward first sync |
| Error: hash generation failed | "Could not generate annotation ID" | Fallback when SHA-256 fails (extremely unlikely) |
| Error: KV write failed | "Sync state could not be saved. Your annotations are safe but sync tracking may be stale." | Problem + reassurance + implication |

---

## Interaction Contracts (Data-Driven)

Phase 3 does not build UI but defines data contracts that constrain how downstream UI behaves.

### Drift Warning (D-09)

| Property | Contract |
|----------|----------|
| Trigger | `currentPlanHash !== storedPlanHash` (SHA-256 comparison) |
| Presentation | Non-blocking warning banner at top of annotation panel |
| User actions | "Sync anyway" (proceed) or "Don't sync" (abort) -- both visible simultaneously, no modal |
| Confirmation | Banner shows both options side-by-side; user must click an explicit choice. No default action, no auto-dismiss. |
| Blocking | Does NOT block sync -- user decides |
| Dismissible | Yes, via "Don't sync" or by proceeding with "Sync anyway" |

### Conflict Resolution (D-12)

| Property | Contract |
|----------|----------|
| Trigger | `annotation.createdA > lastSyncTimestamp AND comment.updated_at > lastSyncTimestamp` |
| Presentation | Modal dialog (per CONTEXT.md specifics section) |
| User actions | "Keep local" / "Keep GitHub" / "Don't sync" |
| Confirmation | Modal displays side-by-side comparison of local annotation text vs GitHub comment text. "Keep GitHub" (destructive -- overwrites local work) requires user to see the comparison before committing. Modal cannot be dismissed without selecting one of the three options. |
| Blocking | Yes -- blocks sync until each conflict is resolved |
| Data shown | Local annotation text vs GitHub comment text (side-by-side) |
| Multiple conflicts | Presented one at a time, sequentially |

### Stable ID Behavior

| Property | Contract |
|----------|----------|
| Format | 12-char lowercase hex string (e.g., `a3f9d2b8c1e4`) |
| Collision suffix | `-1`, `-2`, etc. (e.g., `a3f9d2b8c1e4-1`) |
| Deterministic | Same `blockId + originalText` always produces same base ID |
| Global comments | Use `sha256("global:" + text)` pattern, same 12-char truncation |
| Diff annotations | Remain ephemeral (not stable IDs) -- these are view-only |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | not applicable | not applicable |

No component registry used. Phase 3 is pure TypeScript modules with no UI dependencies beyond existing `packages/ui/types.ts`.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
